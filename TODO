* Planner
 - goal function
 - tree creation: neighbor function
* Interpretation
 - syntax tree to PDDL tree

  * Idea for Heuristics
  How deep is the object we want to move
  -we are sure that this is an underestimate
  -there is probably a better heuristic, but this one should be ok to begin with